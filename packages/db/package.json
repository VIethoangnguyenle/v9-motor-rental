{
  "name": "@v9/db",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "generate": "bun --env-file=../../.env ./node_modules/.bin/drizzle-kit generate",
    "custom": "bun --env-file=../../.env ./node_modules/.bin/drizzle-kit generate --custom",
    "migrate": "bun --env-file=../../.env ./scripts/migrate.ts",
    "studio": "bun --env-file=../../.env ./node_modules/.bin/drizzle-kit studio",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "drizzle-orm": "0.45.2"
  },
  "devDependencies": {
    "drizzle-kit": "0.31.10"
  }
}
